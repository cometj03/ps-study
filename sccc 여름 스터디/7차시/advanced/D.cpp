#include <bits/stdc++.h>
using namespace std;
using ll = long long;
int A[303030];

int main() {
	cin.tie(0)->sync_with_stdio(0);
	ll ans = 0;
	int N;
	cin >> N;
	for (int i = 1; i <= N; i++) cin >> A[i];
}